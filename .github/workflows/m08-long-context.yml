name: M08 long-context routing evaluation

on:
  push:
    branches:
      - feat/m08-long-context-routing
    paths:
      - "src/rag_practice/long_context/**"
      - "src/rag_practice/evaluation/long_context.py"
      - "src/rag_practice/evaluation/long_context_pretrained.py"
      - "benchmarks/m08_long_context/**"
      - "labs/08_specialized_sources/long_context/**"
      - "tests/test_m08_long_context.py"
      - "tests/test_m08_long_context_smollm.py"
      - ".github/workflows/m08-long-context.yml"
      - "pyproject.toml"
      - "ROADMAP.md"
  pull_request:
    paths:
      - "src/rag_practice/long_context/**"
      - "src/rag_practice/evaluation/long_context.py"
      - "src/rag_practice/evaluation/long_context_pretrained.py"
      - "benchmarks/m08_long_context/**"
      - "labs/08_specialized_sources/long_context/**"
      - "tests/test_m08_long_context.py"
      - "tests/test_m08_long_context_smollm.py"
      - ".github/workflows/m08-long-context.yml"
      - "pyproject.toml"
      - "ROADMAP.md"

permissions:
  contents: write

jobs:
  evaluate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
      - name: Install project with full regression dependencies
        run: python -m pip install -e ".[dev,pretrained,visual_document]"
      - name: Run full test suite
        run: pytest -q
      - name: Evaluate deterministic long-context routing
        run: python labs/08_specialized_sources/long_context/run.py
      - name: Evaluate pinned SmolLM2 long-context reader
        run: python labs/08_specialized_sources/long_context/run_smollm.py
      - name: Finalize M08.7 completion docs after successful gate
        if: github.event_name == 'push'
        run: python labs/08_specialized_sources/long_context/finalize_after_gate.py
      - name: Persist long-context evaluation evidence and completion
        if: github.event_name == 'push'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add \
            ROADMAP.md \
            labs/08_specialized_sources/long_context/README.md \
            labs/08_specialized_sources/long_context/results/results.json \
            labs/08_specialized_sources/long_context/results/results.md \
            labs/08_specialized_sources/long_context/results/smollm_results.json \
            labs/08_specialized_sources/long_context/results/smollm_results.md
          if ! git diff --cached --quiet; then
            git commit -m "docs: complete M08.7 long-context routing [skip ci]"
            git push origin HEAD:feat/m08-long-context-routing
          fi
